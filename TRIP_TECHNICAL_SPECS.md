# 行程全链路功能需求文档 (Trip Lifecycle PRD)

本文档详细定义了从**行程创建**、**执行**到**结束**的全链路功能模块及业务逻辑，重点阐述**多源导入**与**冲突检测**的产品规则。

---

## 1. 行程创建阶段 (Trip Creation Phase)

该阶段的核心目标是将用户的模糊意图或分散的票务信息，转化为标准化的**结构化行程数据**。

### 1.1 路径 A：AI 智能规划 (AI Planning)
*   **功能描述**: 用户通过自然语言（语音/文本）表达需求，系统生成完整行程。
*   **业务逻辑**:
    *   **意图识别**: 系统需准确识别用户输入的 `目的地`（如贵阳）、`天数`（3天）、`偏好`（特种兵/慢节奏/亲子）。
    *   **知识库调用**: 结合本地景点库与交通时刻表，辅助 AI 生成合理路线，避免“幻觉”路线。
    *   **标准化输出**: 生成的数据必须符合前端渲染标准，包含每日的时间线、地点坐标及活动详情。
    *   **对话式调整**: 支持用户以对话形式微调行程风格。
        *   *场景示例*: 用户说“太累了，我想轻松点” -> 系统自动减少每日景点数量，增加休息时间。
        *   *实现方式*: 通过调整 AI 的生成参数并重新排序候选方案来实现。

### 1.2 路径 B：多源智能导入 (Multi-source Import)
*   **功能描述**: 支持用户上传第三方平台的订单截图、PDF 行程单或分享链接，系统自动解析并合并到当前行程。支持**批量导入**，即一次上传多张截图或多个文件。
*   **功能细节**:

    | 导入类型 | 核心能力 | 业务处理逻辑 |
    | :--- | :--- | :--- |
    | **图片/截图** | **智能文字识别 (OCR)** | 1. **图像优化**: 自动增强图片清晰度，确保文字可读。<br>2. **关键信息提取**: 自动定位“航班号”、“入住酒店”、“起飞时间”等关键字段。<br>3. **格式化**: 将提取的信息转换为标准的日期和班次格式。<br>4. **批量处理**: 支持多图上传，系统按时间顺序自动合并。 |
    | **分享链接** | **链接智能解析** | 1. **平台识别**: 自动识别链接来源（如携程/飞猪/12306）。<br>2. **内容抓取**: 提取页面中的标题、时间和地点信息。<br>3. **APP唤起**: 若用户安装了对应APP，尝试唤起以获取更精准数据。 |
    | **文件 (PDF)** | **文档结构化解析** | 1. **文本提取**: 读取电子行程单中的所有文本内容。<br>2. **智能映射**: 将非结构化文本映射到行程数据模型中。 |
    | **粘贴文本** | **语义实体识别** | 1. **实体抽取**: 自动识别文本中的 `时间`、`地点`、`机构名` (航空公司/酒店)。<br>2. **信息补全**: 自动补充缺失的年份或城市信息（基于上下文）。 |

### 1.3 冲突检测与处理 (Conflict Detection & Resolution)
在合并新行程节点时，系统必须进行逻辑校验，防止行程安排不合理。

*   **逻辑规则**:
    1.  **时间冲突 (Hard Conflict)**: 新行程的时间段与已有行程完全重叠。
        *   *判定*: 新活动的开始时间早于已有活动的结束时间。
    2.  **地理冲突 (Soft Conflict)**: 两个活动之间距离过远，交通时间不足。
        *   *判定*: 上一个地点的结束位置到新地点的交通耗时 > 两个活动之间的时间间隔。
*   **交互设计**:
    *   **弹窗提示**: “检测到您在 10:00-12:00 已有【黄果树游览】安排，新导入的【高铁票】时间冲突。”
    *   **解决方案**:
        *   **替换**: 用新导入的节点覆盖原有的冲突节点。
        *   **顺延**: 保留原有节点，将新节点及后续行程自动向后顺延。
        *   **并存**: 允许冲突存在（标记为“待定”状态），由用户后续手动调整。

### 1.4 行程列表展示规范 (Trip List Display Specs)
该模块用于集中管理用户的所有行程，支持状态筛选与快捷操作。

*   **UI 构成**:
    *   **列表卡片**:
        *   **地图快照**: 左侧展示行程关键节点的缩略路线图（静态图）。
        *   **核心信息**: 
            *   **标题**: 目的地+天数+主题（如“贵阳·亲子3日游”）。
            *   **时间**: 具体日期段（如 "10.01 - 10.03"）及“3天2晚”标识。
            *   **状态标签**: 
                *   `规划中` (Draft): 灰色标签，未最终确认。
                *   `即将开始` (Upcoming): 蓝色标签，显示倒计时（如“还有2天出发”）。
                *   `进行中` (Active): 绿色呼吸灯标签，显示“正在进行”。
                *   `已完成` (Completed): 灰色标签，显示“去评价”。
    *   **筛选 Tab**: “全部”、“规划中”、“未出行”、“历史行程”。

*   **交互逻辑**:
    *   **点击**: 进入 [1.5 行程详情页]。
    *   **左滑**: 出现“删除”、“重命名”、“分享”快捷菜单。
    *   **长按**: 支持拖拽排序或置顶常用行程。

### 1.5 行程详情页规范 (Trip Detail Page Specs)
行程详情页是用户查看、调整和确认方案的核心载体，采用 **双视图模式** (Overview & Daily) 以满足宏观把控与微观调整的双重需求。

*   **顶部导航栏 (Top Navigation Header)**:
    *   **左侧**: `返回` (Back) 按钮，点击返回行程列表。
    *   **中间**: `视图切换` (View Switching) 胶囊按钮，在 `总览` (Overview) 与 `每日` (Daily) 之间切换。默认选中“总览”。
    *   **右侧**: `更多` (More) 按钮，点击唤起分享、导出、设置等菜单。

#### 1.5.1 总览模式 (Overview Mode)
侧重于行程的整体结构展示与快速筛选。

*   **地图快照 (Map Snapshot)**:
    *   顶部展示 48px 高度的地图预览区域。
    *   **交互**: 点击进入全屏地图模式，展示完整路线轨迹。
*   **筛选过滤器 (Filter Tabs)**:
    *   位于地图下方，支持横向滑动的分类标签。
    *   **选项**: `全部`、`交通`、`餐饮`、`景点`、`住宿`。
    *   **逻辑**: 点击标签，下方时间轴仅展示对应类型的节点，方便用户快速核对（如只看所有酒店）。
*   **全量时间轴 (Full Timeline)**:
    *   按天分组展示所有行程节点。
    *   **Day Header**: 显示 Day 序号、日期、主题 Tag（如“Day 1 | 抵达日”）。
    *   **节点展示**: 使用标准 [1.6 行程节点卡片] 进行渲染。

#### 1.5.2 每日模式 (Daily Mode)
侧重于单日行程的深度编辑与编排。

*   **日期选择器 (Day Selector)**:
    *   顶部吸附的横向滚动条，展示所有日期（Day 1, Day 2...）。
    *   **当前态**: 高亮选中日期；**操作**: 点击 `+` 按钮可追加新的一天。
*   **可编辑表头 (Editable Header)**:
    *   支持直接点击修改当日的“标题”（如“文化探索”）和“标签”。
    *   提供 **AI 规划入口** (Wand Icon)，点击可唤起 AI 针对当日行程的重新规划或补全。
*   **拖拽编排列表 (Reorderable List)**:
    *   展示当日的所有节点。
    *   **交互**: 支持长按拖拽节点调整上下顺序，调整后系统自动重算时间。
    *   **添加节点**: 列表底部提供显眼的 `+ 添加行程节点` 按钮，支持“搜索地点”、“智能导入”、“自定义”三种添加方式。

### 1.6 行程节点卡片生成规范 (Trip Node Card Generation Specs)
针对行程详情页时间轴中每一个具体的“节点”，需根据 POI 类型进行差异化生成，融合静态数据与 AI 动态推荐。

#### 1.6.1 数据构成
每个节点卡片的数据由三层构成：
*   **基础层 (L1)**: 来源于基础数据库。含名称、地址、经纬度、官方图、基础标签（5A/米其林）。
*   **推荐层 (L2)**: 来源于 AI 智能生成。含“推荐理由”（基于用户画像）、“游玩贴士”（基于季节/天气）、“必体验”（如招牌菜）。
*   **状态层 (L3)**: 来源于实时数据接口。含拥堵指数、排队桌数、航班动态、今日闭园时间。

#### 1.6.2 分类生成要求

| 节点类型 | 视觉重点 | 核心字段要求 | AI 生成内容 | 基础服务 (通用) | 定制服务 (需接入智能体) |
| :--- | :--- | :--- | :--- | :--- | :--- |
| **景点** | 沉浸感大图/视频 | 景区等级 (5A)、开放时间、门票价格 | **推荐理由**: "适合亲子，步行少"<br>**拍照点**: "最佳机位在观瀑台" | **地图导航**: 唤起地图APP<br>**电话咨询**: 拨打景区热线 | **在线购票**: 接入票务系统<br>**智能导览**: AR/语音伴游<br>**预约讲解**: 预约金牌导游 |
| **餐饮** | 诱人菜品特写 | 菜系 (酸汤鱼)、人均消费、大众评分 | **必点菜**: "Top3: 酸汤鱼, 米豆腐..."<br>**避雷**: "辣度较高，建议微辣" | **查看位置**: 地图定位<br>**拨打电话**: 联系商家 | **在线订座**: 接入排队/订座系统<br>**查看菜单**: 数字化菜单<br>**外卖点单**: 接入配送服务 |
| **住宿** | 房型/环境图 | 星级/钻级、入住/退房时间、房型名称 | **入住贴士**: "建议20:00前入住"<br>**周边**: "楼下有便利店" | **一键导航**: 酒店定位<br>**联系前台**: 拨打总机 | **办理入住**: 在线选房/CI<br>**客房服务**: 送餐/清洁请求<br>**延住续房**: 快速续单 |
| **交通** | 路线/时刻表 | 班次号 (G1234)、起终点、预计耗时 | **换乘建议**: "站内换乘预留20分钟"<br>**准点率**: "历史准点率 95%" | **路线规划**: 查看行车路线<br>**分享行程**: 发送给亲友 | **在线改签**: 接入航司/铁道系统<br>**接送机**: 预约专车服务<br>**值机选座**: 在线办理值机 |
| **自由活动** | 氛围图/插画 | 所在区域、建议时长 | **推荐活动**: "附近逛街/发呆"<br>**备选方案**: "若下雨可去博物馆" | **查看地图**: 探索周边POI | **添加安排**: 关联具体服务<br>**购买权益**: 购买周边通票 |

#### 1.6.3 动态渲染逻辑
*   **优先级排序**: 若图片缺失，优先展示 POI 类型图标，而非空白占位。
*   **标签自适应**: 根据用户偏好动态高亮标签。例如用户是“摄影爱好者”，则卡片优先展示“出片圣地”标签；若是“亲子游”，优先展示“推车友好”。
*   **状态透出**:
    *   *正常*: 显示评分/人均。
    *   *异常*: 若检测到“今日闭园”或“航班取消”，卡片整体置灰并显示红色 **异常提醒** 标签，同时触发重规划建议。

### 1.7 规划后关联推荐 (Post-Planning Recommendations)
**功能描述**: 当行程规划智能体完成方案输出后，系统自动扫描行程中提及的景点、酒店、美食等关键要素，并精准推送对应的“企业智能体卡片”或“商品购买卡片”，实现从“看方案”到“找服务”的无缝衔接。

*   **业务逻辑**:
    *   **智能实体映射**: 系统自动识别行程文本中的关键实体（如“黄果树瀑布”、“凯里酸汤鱼”），并在后台数据库中检索是否存在对应的**官方认证智能体**或**平台自营商品**。
    *   **优先级排序**:
        1.  **强关联**: 行程中明确提及的具体酒店或收费景区。
        2.  **高热度**: 目的地城市的热门特产或必吃榜餐厅。
        3.  **促销中**: 平台当前正在进行活动的优惠商品。
*   **交互形态**:
    *   **推荐胶囊流**: 在行程详情页底部或概览卡片下方，以横向滑动的形式展示“相关服务”胶囊。
    *   **直连跳转**:
        *   点击 **智能体卡片**（如“黄果树景区智能体”）：直接进入该智能体的对话页面，上下文自动携带“我计划X月X日去黄果树，请介绍一下门票”。
        *   点击 **商品卡片**（如“非遗蜡染体验券”）：直接跳转至统一收银台或商品详情页。

---

## 2. 行程管理与服务 (Trip Management & Services)

该阶段对应“看行程”环节，用户在此阶段对已生成的行程进行全方位的管理、对比与服务获取。

### 2.1 行程对比 (Trip Comparison)
当用户拥有多个候选行程方案时（如“方案A：特种兵版” vs “方案B：休闲版”），系统提供多维度的辅助决策工具。

*   **总览对比 (Overview Comparison)**:
    *   **UI形态**: 
        *   **对比表格**: 以 `维度` 为行，`方案` 为列的矩阵展示。
        *   **视觉增强**: 优胜项 (Winner) 自动标注“更优”标签并高亮显示。
    *   **核心对比字段**:
        *   **行程主题** (Theme): 如“适合亲子”、“适合人文”。
        *   **预估费用** (Budget): 总价对比，数值越低越优。
        *   **行程天数** (Days): 总时长对比。
        *   **总路程** (Distance): 移动距离预估。
        *   **路线效率** (Efficiency): 如“顺路不绕行” vs “轻微绕路”。
        *   **行程强度** (Pace): 如“轻松”、“适中”、“紧凑”。
        *   **特色亮点** (Highlights): 景点数量统计，支持点击查看具体列表。
*   **深度透视 (Deep Dive Comparison)**:
    *   **逐日对比 (Day by Day)**: 按天展示不同方案的行程差异。
    *   **共同包含 (Common Spots)**:
        *   展示两个方案都包含的景点/节点。
        *   显示名称、标签（如“必打卡”）及简短描述。
    *   **差异对比 (Differences)**:
        *   **独有体验 (Gains)**: 标记当前方案独有的节点（绿色高亮）。
        *   **未包含 (Losses)**: 提示相比其他方案错过的体验（灰色提示）。
    *   **亮点详情弹窗 (Highlights Modal)**:
        *   在总览页点击“特色亮点”数值时唤起，列表展示该方案包含的所有核心景点名称。

### 2.2 行程分享 (Trip Sharing)
支持将结构化行程转化为社交友好的格式进行传播。

*   **卡片分享**:
    *   生成长图：包含封面、每日亮点时间轴、二维码（Deep Link）。
    *   **小程序卡片**: 微信内直接唤起小程序打开对应行程。
*   **协同编辑**:
    *   生成“协作口令”或邀请链接，允许同伴加入同一行程进行查看和简单备注。

### 2.3 智能通知与推荐系统 (Smart Notification & Recommendation System)
本模块统一定义了“灵动岛推荐”与“消息提醒”的底层逻辑，实现**逻辑归一，端侧适配**。

#### 2.3.1 统一逻辑层 (Unified Logic Layer)
所有触达行为均基于统一的触发器与优先级队列管理。

*   **触发器类型 (Triggers)**:
    *   **上下文触发 (Contextual)**: 基于时间/地点/状态（e.g., "无行程且在周末" -> 推送热门活动）。
    *   **事件触发 (Event-driven)**: 基于服务状态变更（e.g., "出票成功" -> 推送闭环通知）。
    *   **紧急触发 (Emergency)**: 基于外部不可抗力（e.g., "暴雨预警" -> 推送中断通知）。
*   **优先级队列 (Priority Queue)**:
    1.  **P0 (Critical)**: 紧急安全/阻断性信息（如航班取消）。*行为: 强制打断/弹窗/强震动*。
    2.  **P1 (Service)**: 服务闭环/交易状态（如出票成功、司机到达）。*行为: 扩展展示/红点提示*。
    3.  **P2 (Navigation)**: 行程导航/实时状态（如剩余距离）。*行为: 常驻显示*。
    4.  **P3 (Marketing)**: 营销/建议/关怀（如美食推荐）。*行为: 轮播/静默展示/可关闭*。

#### 2.3.2 自适应表现层 (Adaptive Presentation Layer)
根据运行环境（Runtime Environment）自动渲染对应的 UI 组件。

| 场景 | 优先级 | **App 端 (Native)** | **小程序 端 (Mini-Program)** |
| :--- | :--- | :--- | :--- |
| **营销推荐** | P3 | **灵动岛 (Dynamic Island)**<br>默认收起，图标轮播，点击展开。 | **通告栏/气泡 (Banner/Bubble)**<br>顶部可关闭通告栏或侧边悬浮气泡。 |
| **行程伴随** | P2 | **灵动岛 (Live Activity)**<br>锁屏实时显示，岛内常驻。 | **顶部胶囊 (Top Capsule)**<br>首页顶部常驻胶囊条。 |
| **服务通知** | P1 | **灵动岛弹窗 (Pop-up)**<br>主动展开 3s 后收起 + 系统通知。 | **模态弹窗 (Modal)**<br>应用内弹窗 + 微信服务通知模板消息。 |
| **紧急插队** | P0 | **灵动岛红色警示 (Alert)**<br>持续高亮闪烁 + 强震动。 | **全屏阻断弹窗 (Blocking Alert)**<br>需用户手动确认才可关闭。 |

#### 2.3.3 推荐策略 (Recommendation Strategy)
*   **行前**: 距离出发 7 天 -> 推荐接送机/导游。
*   **行中**: 饭点/景点附近 -> 推荐餐饮/特色体验。
*   **行后**: 行程结束 -> 推荐特产/评价。

---

## 3. 行程执行阶段 (Trip Execution Phase)

该阶段的核心是**状态自动流转**与**上下文感知**。

### 3.1 状态流转与启动机制 (State Transition & Activation)

行程状态流转不再仅依赖系统时间，引入用户**主动确认**机制，确保行程开始的准确性。

*   **启动机制 (Activation)**:
    *   **动作**: 用户在行程详情页点击 `开始行程` 按钮。
    *   **输入**: 弹出时间选择器，用户确认/修改 `实际启动时间` (默认填充为计划开始时间)。
    *   **逻辑**: 
        *   若 `当前时间` < `启动时间`: 状态流转为 `预热中 (Warming-up)`。
        *   若 `当前时间` >= `启动时间`: 状态流转为 `进行中 (Active)`，灵动岛立即接管。

*   **流转阶段 (Phases)**:
    *   **预热期 (Warming-up)**: 启动时间前 **2小时** (可配置) 进入此状态。
        *   *行为*: 首页倒计时卡片变更为“准备出发”状态，推送路况检查。
    *   **行中 (In-trip)**:
        *   *行为*: 灵动岛常驻，根据 GPS + 时间轴自动切换当前节点。
    *   **行后 (Post-trip)**:
        *   *行为*: 最后一个节点结束后 4 小时自动归档。

### 3.2 动态调度 (Dynamic Scheduling)
*   **场景**: 用户在游玩过程中临时改变计划（如“游览超时”）。
*   **业务逻辑**:
    *   **连锁更新**: 当用户手动推迟当前节点的结束时间 -> 系统触发智能重排算法 -> 自动推迟后续节点的开始时间。
    *   **智能建议**: 若推迟导致后续景点无法在闭园前到达，系统自动建议“取消该节点”或“改期”。

### 3.3 场景化消息推送规则 (Scenario-based Push Rules)
针对不同类型的行程节点与时间点，定义精细化的推送策略。

| 节点类型 | 触发时机 (Timing) | 消息类型 (Priority) | 推送内容 (Content) | 动作建议 (Action) |
| :--- | :--- | :--- | :--- | :--- |
| **全局行程** | **启动时间 - 24小时** | 提醒 (P3) | 天气预警与行李清单 | "查看打包建议" |
| **全局行程** | **启动时间 - 2小时** | 提醒 (P2) | 首站路况与交通耗时预估 | "开始导航" |
| **大交通**<br>(机票/火车) | **起飞/发车 - 4小时** | 提醒 (P1) | 出发提醒 (航站楼/检票口) | "预约送机" |
| **大交通** | **状态变更 (延误/取消)** | **紧急 (P0)** | 航班/列车异常变动 | "改签/退票" |
| **大交通** | **抵达目的地** | 推荐 (P2) | 落地欢迎与接驳建议 | "打车/网约车" |
| **住宿** | **入住日 - 14:00** | 服务 (P2) | 办理入住提醒 (含预订号) | "一键办理入住" |
| **住宿** | **离店日 - 09:00** | 提醒 (P3) | 退房提醒与行李寄存建议 | "预约延时退房" |
| **景点** | **抵达围栏 (Geofence)** | 服务 (P1) | 自动弹出入园码/电子票 | "扫码入园" |
| **景点** | **游玩中 (In-progress)** | 推荐 (P3) | 智能导览与语音讲解推荐 | "听讲解" |
| **餐饮** | **预约时间 - 1小时** | 提醒 (P2) | 用餐提醒与排队进度 | "查看排队号" |
| **餐饮** | **抵达餐厅** | 服务 (P1) | 扫码点餐或核销套餐 | "点餐/核销" |

> **注**: 所有 P0/P1 级消息在 App 端均通过灵动岛弹窗展示，小程序端通过模态弹窗展示；P2/P3 级消息以静默通知或轮播形式展示。

---

## 4. 行程结束阶段 (Trip Completion Phase)

### 4.1 自动结项与归档 (Auto-Completion & Archiving)
*   **触发条件**:
    1.  所有节点状态均为 `已完成`。
    2.  当前时间 > 行程最后节点的结束时间 + 缓冲期 (e.g., 4小时)。
*   **数据处理**:
    *   将行程状态从 `进行中` 移至 `历史行程`。
    *   冻结行程数据，不再接受即时修改（需“重新激活”才能修改）。

### 4.2 智能回忆与总结 (Smart Memory & Summary)
*   **行程总结报告**:
    *   **自动生成**: 结合用户的轨迹、照片、消费记录，AI 生成一份“旅行总结”。
    *   **内容包含**: “足迹地图”、“消费账单”、“打卡成就”（如：解锁了3个5A景区）。
    *   **分享**: 支持一键生成海报分享至朋友圈。
*   **回忆视频**: 自动剪辑相册中的高光时刻，配以背景音乐。

### 4.3 服务评价闭环 (Feedback Loop)
*   **多维度评价体系**:
    *   **对平台**: 评价“黄小西”的行程规划能力（如：路线是否合理、推荐是否精准）。
    *   **对服务商**: 针对行程中涉及的每一个具体服务节点（酒店、餐厅、导游）进行单独评价。
*   **数据流转**:
    *   差评 -> 实时触发工单 -> 推送至商户端/运营后台 -> 启动售后介入。
    *   好评 -> 累积至商户信用分 -> 提升其在推荐算法中的权重。
