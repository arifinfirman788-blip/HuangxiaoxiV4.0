---
description: 每次修改代码完成后自动触发热更新或刷新
alwaysApply: true
---

# 修改完成后自动刷新

每次完成代码修改后，自动执行以下操作之一以帮助用户看到最新效果：

1. **优先触发热更新**：对刚修改的文件执行 `touch <文件路径>`，在当前已运行端口直接热更新
2. **仅在未运行时启动**：若开发服务器未运行，才执行 `npm run dev`
3. **避免切换端口**：不要反复重启服务导致端口变化；优先复用当前打开页面对应端口
4. **自动刷新提示**：热更新后提示用户页面已刷新；若未自动刷新，提示 Cmd+R / Cmd+Shift+R

优先使用 touch 触发热更新，无需重启服务器。
