# 行程状态流程图

## 一、整体状态流转（我的行程 → 首页卡片 → 详情页节点）

```mermaid
flowchart TD
    Start([用户进入系统]) --> CheckActive{"是否有<b>进行中</b>的行程?"}
    
    CheckActive -->|无| ShowPrompt["首页行程提醒卡片显示:<br/>暂无进行中行程"]
    CheckActive -->|有| CalcNodeStatus["根据当前时间<br/>计算所有节点状态"]

    CalcNodeStatus --> FindOngoing{"是否存在<b>进行中</b>的节点?"}
    
    FindOngoing -->|是| ShowOngoingNode["首页行程提醒卡片<br/>展示该进行中的节点"]
    FindOngoing -->|否-全部未开始| ShowFirstNode["首页行程提醒卡片<br/>展示第一个即将开始的节点<br/>状态: 未开始"]
    FindOngoing -->|否-当天已结束| ShowNextDay["首页行程提醒卡片<br/>展示下一天第一个节点<br/>状态: 未开始"]
    FindOngoing -->|否-所有节点已完成| CheckAutoEnd{"最后一天最后节点<br/>是否已结束?"}
    
    CheckAutoEnd -->|是| AutoComplete["行程自动标记为<b>已完成</b><br/>首页显示: 暂无进行中行程"]
    CheckAutoEnd -->|否| ShowPrompt
```

## 二、我的行程 — 开始/结束行程操作流

```mermaid
flowchart TD
    Entry([进入我的行程页面]) --> ListTrips["展示行程列表"]
    
    ListTrips --> TripState{"行程当前状态?"}
    
    TripState -->|计划中| PlanUI["卡片左上角: 计划中<br/>显示<b>开始行程</b>按钮<br/>✅ 支持编辑行程"]
    TripState -->|进行中| ActiveUI["卡片左上角: 进行中<br/>显示<b>提前结束</b>按钮<br/>❌ 禁止编辑行程"]
    TripState -->|已完成| DoneUI["卡片左上角: 已完成<br/>无操作按钮<br/>❌ 禁止编辑行程"]

    PlanUI --> ClickStart["用户点击<b>开始行程</b>"]
    ClickStart --> CheckConflict{"是否有其他<br/>进行中的行程?"}
    
    CheckConflict -->|有| BlockStart["❌ 阻止开启<br/>提示: 请先结束当前行程"]
    CheckConflict -->|无| SelectTime["弹窗: 选择出发时间"]
    
    SelectTime --> ValidateTime{"选择时间 > 当前时间?"}
    ValidateTime -->|否| TimeError["提示: 请选择当前时间之后的时间"]
    ValidateTime -->|是| ConfirmStart["确认开启"]
    
    ConfirmStart --> StartSync["同步动作"]
    StartSync --> S1["1. 卡片左上角 → 进行中"]
    StartSync --> S2["2. 卡片显示开始时间"]
    StartSync --> S3["3. 禁用行程编辑"]
    StartSync --> S4["4. 首页卡片 → 根据时间<br/>展示对应状态的节点"]
    StartSync --> S5["5. 详情页 → 根据当前时间<br/>计算所有节点状态"]
    
    ActiveUI --> ClickEnd["用户点击<b>提前结束</b>"]
    ClickEnd --> ConfirmEnd{"二次确认:<br/>确定要提前结束?"}
    ConfirmEnd -->|取消| ActiveUI
    ConfirmEnd -->|确定| EndSync["同步动作"]
    
    EndSync --> E1["1. 卡片左上角 → 已完成"]
    EndSync --> E2["2. 首页卡片 → 暂无进行中行程"]
    EndSync --> E3["3. 详情页所有节点 → 已完成"]
    EndSync --> E4["4. 停止行程提醒"]
```

## 三、详情页节点状态计算规则（核心算法）

```mermaid
flowchart TD
    TripActive(["行程状态 = 进行中"]) --> GetNodes["获取所有节点<br/>按天+时间排序:<br/>A(t1), B(t2), C(t3)..."]
    
    GetNodes --> GetNow["获取当前时间 now"]
    GetNow --> LoopStart["遍历每个节点"]
    
    LoopStart --> CompareTime{"now vs 当前节点时间?"}
    
    CompareTime -->|"now < 第一个节点 t1"| AllWaiting["所有节点: <b>未开始</b><br/>首页展示第一个节点"]
    
    CompareTime -->|"t(n) ≤ now < t(n+1)"| SplitStatus["节点1 ~ 节点n-1: <b>已完成</b> ✅<br/>节点n: <b>进行中</b> 🔵<br/>节点n+1 ~ 末尾: <b>未开始</b><br/>首页展示节点n"]
    
    CompareTime -->|"now ≥ 最后节点时间"| LastNodeCheck{"是否当天最后一个节点?"}
    
    LastNodeCheck -->|"是 且 now < 当天24:00"| LastOngoing["之前所有节点: <b>已完成</b> ✅<br/>最后节点: <b>进行中</b> 🔵<br/>首页展示最后节点"]
    
    LastNodeCheck -->|"是 且 now ≥ 当天24:00"| DayDone["当天所有节点: <b>已完成</b> ✅"]
    
    DayDone --> HasNextDay{"还有下一天的行程?"}
    HasNextDay -->|有| NextDayCalc["对下一天节点<br/>重复以上判断逻辑"]
    HasNextDay -->|无-整个行程最后一天| TripAutoEnd["所有节点: <b>已完成</b> ✅<br/>行程自动标记已完成<br/>首页: 暂无进行中行程"]
    
    NextDayCalc --> CompareTime
```

### 节点状态计算伪代码

```
function calcNodeStatuses(allNodes, now):
    currentNode = null
    
    for i = 0 to allNodes.length - 1:
        node = allNodes[i]
        nextNode = allNodes[i + 1]  // 可能为 null
        
        if now < node.startTime:
            // 当前时间早于该节点 → 该节点及之后全部"未开始"
            node.status = "未开始"
            // 后续节点也全部"未开始"（可 break）
            
        else if nextNode == null || now < nextNode.startTime:
            // 当前时间 ≥ 该节点时间，且 < 下一个节点时间
            // → 该节点"进行中"
            node.status = "进行中"
            currentNode = node
            
            // 检查跨天: 如果该节点是当天最后一个，且 now ≥ 24:00
            if isDayLastNode(node) && now >= endOfDay(node.date):
                node.status = "已完成"
                currentNode = null
            
        else:
            // 当前时间 ≥ 该节点时间，且 ≥ 下一个节点时间
            // → 该节点"已完成"
            node.status = "已完成"
    
    return currentNode  // 首页展示该节点
```

## 四、首页行程提醒卡片逻辑

```mermaid
flowchart TD
    HomeEntry([首页加载/刷新]) --> CheckTrip{"是否有<b>进行中</b>的行程?"}
    
    CheckTrip -->|否| ShowEmpty["显示提示: <b>暂无进行中行程</b>"]
    CheckTrip -->|是| CalcStatus["调用节点状态计算<br/>找到 currentNode"]
    
    CalcStatus --> HasCurrent{"currentNode 是否存在?"}
    
    HasCurrent -->|否-全部未开始| ShowUpcoming["展示第一个节点<br/>状态标签: 即将开始"]
    HasCurrent -->|否-全部已完成| TripDone["行程自动完成<br/>显示: 暂无进行中行程"]
    HasCurrent -->|是| NodeType{"currentNode 类型?"}
    
    NodeType -->|航班 flight| ShowFlight["航班卡片<br/>航班号 / 起降地 / 时间"]
    NodeType -->|高铁 transport| ShowTrain["高铁卡片<br/>车次 / 起终点 / 时间"]
    NodeType -->|自驾 transport| ShowDrive["自驾卡片<br/>起终点 / 导航信息"]
    NodeType -->|美食 food/group_meal| ShowFood["美食卡片<br/>美食名称 / 相关信息"]
    NodeType -->|景区 scenic| ShowScenic["景区卡片<br/>景点名 / 地址"]
    NodeType -->|酒店 hotel| ShowHotel["酒店卡片<br/>酒店名 / 位置"]
    NodeType -->|自定义活动 free_time| ShowFree["自定义活动卡片<br/>活动名 / 描述"]
```

## 五、状态对照表

```mermaid
stateDiagram-v2
    [*] --> 计划中: 用户创建/导入行程
    计划中 --> 进行中: 点击"开始行程" + 选择时间
    进行中 --> 已完成: 点击"提前结束" 或 所有节点自然结束
    
    state 计划中 {
        [*] --> 可编辑
        可编辑: ✅ 支持编辑行程内容
        可编辑: 🚫 首页不显示行程卡片
        可编辑: ⏸️ 详情页节点无状态流转
    }
    
    state 进行中 {
        [*] --> 不可编辑
        不可编辑: ❌ 禁止编辑行程
        不可编辑: 📍 首页显示当前节点
        不可编辑: ⏱️ 详情页节点按时间自动流转
        不可编辑: ⚠️ 同一时间仅允许一个行程进行中
    }
    
    state 已完成 {
        [*] --> 归档
        归档: 🚫 首页显示"暂无进行中行程"
        归档: ✅ 详情页所有节点标记已完成
        归档: 🔓 可重新开始（是否支持?）
    }
```

## 六、节点状态时间线示例

```
假设行程已开始，节点按时间排列:

Day 1: A(08:00) → B(10:00) → C(12:30) → D(14:00)
Day 2: E(08:30) → F(12:30) → G(18:00)
Day 3: H(10:00) → I(16:00)

━━━ Day 1 ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
now = 07:00  │ A未开始  B未开始  C未开始  D未开始  │ 首页→A(即将开始)
now = 08:00  │ A进行中🔵 B未开始  C未开始  D未开始  │ 首页→A
now = 09:30  │ A进行中🔵 B未开始  C未开始  D未开始  │ 首页→A
now = 10:00  │ A已完成✅ B进行中🔵 C未开始  D未开始  │ 首页→B
now = 12:30  │ A已完成✅ B已完成✅ C进行中🔵 D未开始  │ 首页→C
now = 14:00  │ A已完成✅ B已完成✅ C已完成✅ D进行中🔵 │ 首页→D
now = 24:00  │ A已完成✅ B已完成✅ C已完成✅ D已完成✅ │ 当天结束

━━━ Day 2 ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
now = 07:00  │ E未开始  F未开始  G未开始            │ 首页→E(即将开始)
now = 08:30  │ E进行中🔵 F未开始  G未开始            │ 首页→E
now = 12:30  │ E已完成✅ F进行中🔵 G未开始            │ 首页→F
now = 18:00  │ E已完成✅ F已完成✅ G进行中🔵           │ 首页→G
now = 24:00  │ E已完成✅ F已完成✅ G已完成✅            │ 当天结束

━━━ Day 3 ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
now = 10:00  │ H进行中🔵 I未开始                     │ 首页→H
now = 16:00  │ H已完成✅ I进行中🔵                    │ 首页→I
now = 24:00  │ H已完成✅ I已完成✅                     │ 行程自动完成
                                                     │ 首页→暂无进行中行程
```

## 七、关键联动关系

```
┌──────────────────┐      ┌──────────────────┐      ┌──────────────────┐
│    我的行程       │      │    首页卡片       │      │    详情页         │
│   (Trip页面)      │      │   (Home页面)      │      │  (TripDetail)    │
├──────────────────┤      ├──────────────────┤      ├──────────────────┤
│                  │      │                  │      │                  │
│ 计划中           │──▶   │ 暂无进行中行程    │      │ 节点无状态流转    │
│                  │      │                  │      │                  │
│ 开始行程         │──▶   │ 展示当前进行中/   │──▶   │ 节点按时间        │
│ (进行中)         │      │ 即将开始的节点    │      │ 自动流转状态      │
│                  │      │                  │      │                  │
│ 提前结束         │──▶   │ 暂无进行中行程    │──▶   │ 所有节点          │
│ (已完成)         │      │                  │      │ 标记已完成        │
│                  │      │                  │      │                  │
│ 自然结束         │──▶   │ 暂无进行中行程    │──▶   │ 所有节点          │
│ (最后节点完成)    │      │                  │      │ 标记已完成        │
└──────────────────┘      └──────────────────┘      └──────────────────┘
```

## 八、已识别的逻辑漏洞及补充

| # | 漏洞描述 | 补充方案 |
|---|---------|---------|
| 1 | 原流程仅判断"是否有行程"，未区分行程状态 | 改为判断"是否有**进行中**的行程" |
| 2 | 节点状态仅对比"="（精确相等），忽略了时间区间 | 改为区间判断：t(n) ≤ now < t(n+1) 时，节点n为进行中 |
| 3 | 结束行程后首页未明确显示内容 | 结束后首页显示"暂无进行中行程" |
| 4 | 未处理"行程自然结束"场景 | 最后一天最后节点完成后，行程自动标记已完成 |
| 5 | 未限制同时只能有一个进行中行程 | 开始新行程前检查是否有其他进行中行程，有则阻止 |
| 6 | 首页未处理"全部未开始"的情况 | 当所有节点未开始时，展示第一个节点并标注"即将开始" |
| 7 | 未考虑跨天间隙（当天结束到次日第一个节点之间） | 当天24:00后当天节点全部完成，次日节点根据时间判断 |
| 8 | 首页卡片类型遗漏了自定义活动和团餐 | 补充 free_time 和 group_meal 类型的卡片展示 |
| 9 | 未说明"已完成"行程是否可重新开始 | 需产品确认：已完成行程是否支持重新开启 |
