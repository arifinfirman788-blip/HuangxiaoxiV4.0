# 黄小西 v3.0 项目功能拆解

本文档详细拆解了“黄小西 v3.0”项目的功能层级。按照“第一层级（首页/模块）- 第二层级（子模块/页面）- 第三层级（组件/列表）- 第四层级（具体操作/详情）”的结构进行组织。

---

## 1. 首页 (Home)
**路径**: `/`

### 1.1 全局底部导航 (Bottom Navigation)
- **功能**: 应用全局的主要导航入口，常驻于页面底部。
- **层级拆解**:
    - `Button` (首页): 点击跳转至首页。
    - `Button` (行程): 
        - **交互逻辑**: 点击跳转至 **[行程中心]** (`/trip`)。
        - **引导逻辑**: 
            - 若当前无行程（首次使用），自动触发引导教程 (Guide Tutorial)。
            - **Step 1**: 黑色半透明遮罩覆盖全屏，高亮显示底部的 **[核心操作 +]** 按钮，并弹出提示气泡“点击这里开启行程”。
    - `Button` (核心操作 +): 悬浮按钮，点击后展开快捷功能菜单（带旋转动画）。
        - **菜单项**:
            - `Button` (创建新行程): 
                - **交互逻辑**: 点击跳转至 **[行程规划]** (`/chat-planning`) 页面。
                - **业务流程**: 系统初始化智能行程规划师，发送欢迎语，引导用户输入目的地和偏好，开启对话式规划流程。
            - `Button` (智能导入地点/行程): 
                - **交互逻辑**: 点击唤起 **[导入弹窗]** (ImportModal)。
                - **引导逻辑**: 若在引导模式下，自动高亮此按钮并提示“第二步：选择导入行程”。
                - **业务流程**:
                    1. **输入阶段**: 用户可在输入框粘贴攻略链接/文本，或点击上传按钮上传行程单/截图。
                        - *引导演示*: 系统会自动填充一段小红书攻略文本和模拟文件，演示导入效果。
                    2. **解析阶段**: 点击“开始识别”，显示 Loading 动画，AI 解析内容。
                    3. **冲突解决** (如有): 若检测到冲突（如住宿地点不一致），弹出冲突解决卡片，用户需手动选择保留项。
                    4. **结果确认**: 展示解析生成的行程预览卡片。
                    5. **完成导入**: 点击确认，弹出“太棒了”成功动画，自动跳转至 **[行程中心]**，新行程置顶显示。
    - `Button` (优选): 点击跳转至 **[优选商城]** (`/shop`)。
    - `Button` (我的): 点击跳转至 **[个人中心]** (`/profile`)。

### 1.2 顶部导航与状态区
- **功能**: 展示用户状态、定位及全局入口。
- **层级拆解**:
    - **位置/天气信息**: 展示当前位置（如“贵州·遵义”）和天气。
    - **全局搜索栏**:
        - `Input`: 点击输入关键词，跳转至搜索结果页（目前仅为占位）。
        - `Button` (麦克风): 点击唤起语音助手监听状态。
    - **消息入口**:
        - `Button` (铃铛图标): 点击跳转至 **[消息中心]** (`/message`)，如有未读消息显示红点。
    - **扫码入口**:
        - `Button` (扫码图标): 点击唤起摄像头进行二维码扫描（模拟）。

### 1.3 动态岛/营销推荐 (Dynamic Island)
- **功能**: 智能推荐营销信息或重要通知。
- **层级拆解**:
    - **营销卡片**:
        - **交互逻辑**: 
            - 点击 `Button` (查看详情): 跳转至对应的活动详情页或营销落地页。
            - 点击 `Button` (不感兴趣): 卡片执行收起动画并消失，记录用户负反馈。

### 1.4 核心功能菜单 (Grid Menu)
- **功能**: 快速访问应用核心模块。
- **层级拆解**:
    - `Button` (我的行程): 点击跳转至 **[行程中心]** (`/trip`)。
    - `Button` (商城): 点击跳转至 **[特产商城]** (`/shop`)。
    - `Button` (消息): 点击跳转至 **[消息中心]** (`/message`)。
    - `Button` (个人中心): 点击跳转至 **[个人中心]** (`/profile`)。

### 1.5 资讯播报 (News Marquee)
- **功能**: 滚动展示最新文旅资讯或预警。
- **层级拆解**:
    - `Button` (资讯条): 点击跳转至 **[资讯与预警]** (`/news`) 页面，自动定位到对应的新闻详情。

### 1.6 当前行程卡片 (Trip Card)
- **功能**: 展示当前正在进行或即将开始的行程。
- **层级拆解**:
    - **交互逻辑**:
        - 点击卡片整体或 `Button` (查看详情): 跳转至 **[行程详情]** (`/trip/:id`)。
        - 点击 `Button` (智能调整): 唤起智能体对话框，针对该行程进行增删改操作。

### 1.7 智能体服务区
- **功能**: 推荐或快速访问各类垂直领域智能体。
- **层级拆解**:
    - **智能体卡片**: 
        - **交互逻辑**: 点击 `Button` (立即沟通): 
            1. 触发分屏模式 (Split View)，主页内容左移。
            2. 右侧滑出 **[智能分屏工作台]** (Agent Workspace)。
            3. 对应智能体（如酒店智能体）自动发送问候语，建立连接。
        - **多模态交互**: 
            - **文本**: 发送/接收文字消息。
            - **语音**: 支持语音输入（Mic）和语音播报（TTS）。
            - **富媒体卡片**: 
                - **酒店服务卡**: 展示房型轮播图（支持点击放大查看）、价格标签、设施Tag；支持表单自动填充（姓名/电话）和一键预订。
                - **餐饮服务卡**: 展示座位类型（散台/包间）图片及容纳人数；支持在线排队取号。
                - **视频播放**: 某些场景下（如景区介绍）支持弹出视频播放器模态框。

---

## 2. 资讯与预警 (News)
**路径**: `/news`

### 2.1 顶部导航
- **层级拆解**:
    - `Button` (返回): 返回上一页。
    - **标题**: 显示“资讯与预警”。

### 2.2 内容切换 (Tabs)
- **层级拆解**:
    - `Button` (最新资讯): 切换至资讯列表视图。
    - `Button` (官方预警): 切换至预警列表视图（含红点提示）。

### 2.3 资讯列表 (News List)
- **层级拆解**:
    - **资讯卡片 (News Card)**:
        - **展示内容**: 封面图、标题、摘要、发布时间。
        - **交互逻辑**: 点击卡片，跳转至新闻详情页（目前为模拟数据，暂无独立详情页，通常会展示富文本内容）。

### 2.4 预警列表 (Alert List)
- **层级拆解**:
    - **预警卡片 (Alert Card)**:
        - **展示内容**: 区域、预警等级（颜色区分）、发布单位、时间。
        - **交互逻辑**: 点击卡片展开/弹窗显示详细的预警指导建议（如“请减少户外活动”、“注意防滑”）。

---

## 3. 行程中心 (Trip)
**路径**: `/trip`

### 3.1 行程概览列表
- **层级拆解**:
    - **历史/规划行程卡片**:
        - `Button` (卡片主体): 点击进入 **[行程详情]** (`/trip/:id`)。
    - `Button` (创建新行程): 点击跳转至 **[行程规划]** (`/chat-planning`)。

### 3.2 行程详情 (Trip Detail)
**路径**: `/trip/:id`
- **层级拆解**:
    - **时间轴节点列表**:
        - **交通节点 (Flight/Train)**:
            - `Button` (电子登机牌/车票): 
                - **交互逻辑**: 点击弹出模态框，展示二维码/条形码、座位号、登机口等关键信息，再次点击或点击背景关闭。
            - `Button` (遇到问题): 
                - **交互逻辑**: 唤起 AI 客服对话框，预设“航班延误”、“改签”等快捷问题。
        - **酒店节点 (Hotel)**:
            - `Button` (一键导航): 唤起第三方地图应用（如高德/百度）进行导航。
            - `Button` (联系前台): 唤起系统拨号盘，预填酒店电话。
            - `Button` (客房服务): 
                - **交互逻辑**: 触发分屏模式，右侧加载酒店服务智能体，提供点餐、送物等服务菜单。
        - **景区节点 (Scenic)**:
            - `Button` (地图导览): 跳转至景区手绘地图/GIS地图页面。
            - `Button` (购票/预约): 跳转至购票详情页或唤起小程序购票插件。
        - **餐饮节点 (Food)**:
            - `Button` (一键导航): 唤起地图导航。
            - `Button` (排队取号): 
                - **交互逻辑**: 弹出排队信息确认框（当前号码、预计等待时间），点击确认后加入排队队列。
    - **底部操作栏**:
        - `Button` (调整行程): 进入行程编辑模式，可拖拽节点排序或删除节点。
        - `Button` (分享行程): 
            - **交互逻辑**: 弹出分享选项（生成长图、微信好友、复制链接），生成一张包含行程概览的精美海报图片。

---

## 4. 智能体广场 (Agent Square)
**路径**: `/agent-square`

### 4.1 搜索与筛选
- **层级拆解**:
    - **搜索栏**: 
        - `Input`: 输入关键词实时过滤下方智能体列表。
    - **分类导航 (Categories)**:
        - `Button` (食/宿/行/游/购/娱): 点击高亮选中，列表仅展示对应类别的智能体。

### 4.2 智能体列表
- **层级拆解**:
    - **智能体卡片**:
        - `Button` (卡片主体): 点击进入 **[智能体主页]**。
        - `Button` (添加/收藏): 点击切换收藏状态（空心星 -> 实心星），收藏后会在“我的”页面显示。

### 4.3 智能体详情页 (示例: 景区智能体)
**路径**: `/agent/scenic/:id`
- **层级拆解**:
    - **顶部展示区**: 景区图片、基本信息、评分。
    - **功能快捷入口**:
        - `Button` (购票): 弹出购票选择弹窗（成人/儿童/日期）。
        - `Button` (导览): 开启语音导览播放器，自动播放当前景点讲解。
        - `Button` (找厕所): 在地图上标记并导航至最近的卫生间。
    - **对话交互区**:
        - `Input`: 输入文字发送给智能体。
        - `Button` (语音): 按住说话，松开后自动转文字并发送。

---

## 5. 消息中心 (Message)
**路径**: `/message`

### 5.1 消息列表
- **层级拆解**:
    - **顶部Tabs**:
        - `Button` (对话消息): 切换至用户与智能体/商家的聊天列表，按时间倒序排列。
        - `Button` (系统通知): 切换至系统级通知列表（如订单状态、活动提醒）。
    - **对话列表项**:
        - `Button` (列表项): 点击跳转至 **[消息详情]** (`/message/:id`)。

### 5.2 消息详情 (Chat)
**路径**: `/message/:id`
- **层级拆解**:
    - **聊天记录流**: 展示文本、卡片（行程卡片/服务卡片）、图片消息。
    - **输入区**:
        - `Input`: 文本输入框。
        - `Button` (发送): 点击发送当前输入内容。
        - `Button` (更多): 弹出多媒体菜单（图片/位置/文件）。

---

## 6. 个人中心 (Profile)
**路径**: `/profile`

### 6.1 用户信息区
- **层级拆解**:
    - **头像/昵称**: 点击可进入个人资料编辑页（未实现）。
    - **认证标识**: 展示实名认证/达人认证状态，点击可查看认证详情。

### 6.2 电子名片 (Business Card)
- **功能**: 管理和展示个人电子名片。
- **层级拆解**:
    - **名片预览卡片**:
        - `Button` (编辑名片): 
            - **交互逻辑**: 点击弹出 **[名片编辑模态框]** (BusinessCardModal)。
            - **业务流程**:
                1.  **表单填写**: 用户修改头像、背景图、姓名、职位、公司、简介等信息。
                2.  **保存**: 点击“保存”按钮，前端校验数据有效性，更新本地存储状态，关闭弹窗并刷新预览区域。
        - `Button` (分享名片): 
            - **交互逻辑**: 点击弹出 **[分享选项]**。
            - **业务流程**:
                - `Button` (生成海报): 将当前名片样式渲染为图片并保存到相册。
                - `Button` (微信分享): 唤起微信 SDK 分享给好友或朋友圈（模拟弹窗）。

### 6.3 功能菜单列表
- **层级拆解**:
    - `Button` (我的订单): 跳转至订单列表页，按状态筛选（全部/待支付/待使用）。
    - `Button` (我的钱包): 跳转至钱包页，展示余额及充值入口。
    - `Button` (常用联系人): 跳转至联系人管理页，支持增删改查。
    - `Button` (设置): 跳转至系统设置页（通用/隐私/关于）。
    - `Button` (退出登录): 
        - **交互逻辑**: 弹出二次确认框，确认后清除本地 Session/Token，跳转至登录页 (`/login`)。

---

## 7. 智能分屏工作台 (Agent Workspace)
**功能**: 双屏模式下的右侧智能体服务窗口。
- **层级拆解**:
    - **顶部栏**:
        - `Button` (关闭): 点击收起右侧分屏，恢复单屏模式。
        - `Button` (切换模式): 点击在 "AI 智能模式" 和 "人工客服模式" 之间切换。
    - **内容区**:
        - 动态展示服务详情（如酒店房型列表、景区门票预订页），支持滚动浏览。
    - **交互区**:
        - `Input`: 输入针对当前服务的具体需求。
        - `Button` (确认/支付): 
            - **交互逻辑**: 点击发起订单创建流程，弹出支付确认框，支付成功后更新订单状态并发送通知。

---

## 8. 优选商城 (Shop)
**路径**: `/shop`

### 8.1 沉浸式头部
- **层级拆解**:
    - **标题**: 显示 "SHOP."。
    - **操作区**:
        - `Button` (搜索): 点击展开全屏搜索页，支持热门搜索词推荐。
        - `Button` (购物车): 点击进入购物车页面，展示已选商品及结算栏。

### 8.2 营销推荐区
- **层级拆解**:
    - **主推海报 (Hero Section)**:
        - 展示新品或联名活动（如“村超联名款”）。
        - `Button` (立即选购): 点击跳转至该活动的专题落地页。
    - **推荐模块**:
        - `Card` (甄选榜单): 点击跳转至热销商品排行榜。
        - `Card` (限时特惠): 点击跳转至限时折扣专场。

### 8.3 商品分类与列表
- **层级拆解**:
    - **分类导航**:
        - `Button` (全部/甄选好物/非遗文创/地道美食/精品线路): 点击切换下方商品列表的数据源，并带有胶囊指示器动画。
    - **商品瀑布流**:
        - **商品卡片**:
            - **展示**: 图片、标签（甄选/非遗）、标题、价格、评分。
            - `Button` (收藏): 点击切换收藏图标状态，加入收藏夹。
            - **交互逻辑**: 点击卡片主体，跳转至 **[商品详情页]**，展示商品轮播图、详细参数、评价及购买栏。
