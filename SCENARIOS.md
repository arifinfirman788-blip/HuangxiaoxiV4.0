# 功能场景拆分说明 (Feature Scenarios)

本文档对 `HuangxiaoxiV3.0` 项目中的核心功能场景进行拆分与说明，涵盖灵动岛、智能体/名片生成、行程全链路及商品交易等主线。

---

## 1. 智能通知与推荐场景 (Smart Notification & Recommendation)

本场景展示了“智能通知与推荐系统”在 **App 端** 的核心实现——**灵动岛组件** (`src/components/DynamicIsland.jsx`)。该组件根据用户状态智能切换，实现“逻辑归一，端侧适配”（注：小程序端对应为顶部通告栏与胶囊，逻辑同理）。

### 1.1 无行程状态：营销轮播 (Marketing Loop)
*   **触发条件**：用户未创建或未开始任何行程 (`trip` 数据为空)。
*   **功能表现** (App端-灵动岛):
    *   **自动轮播**：系统每隔 **10秒** 自动切换推荐内容（如：当季景区推荐、酒店特惠、美食打卡）。
    *   **消息通知**：每次切换时，灵动岛会弹出 Toast 通知，持续 **4秒**，提示用户点击查看。
    *   **交互**：点击灵动岛可展开卡片，查看推荐详情（标题、描述、图标），并提供“查看详情”或“不感兴趣”的操作按钮。
*   **数据流**：使用预设的 `PROACTIVE_MESSAGES` 常量数组进行循环展示。

### 1.2 行程进行状态：智能导航 (Trip Navigation)
*   **触发条件**：用户已采纳并开启了一个行程 (`trip.itinerary` 存在)。
*   **功能表现** (App端-灵动岛):
    *   **节点推进**：系统模拟行程推进，每隔 **8秒** 自动切换到下一个行程节点（如：从“酒店”移动到“景区”）。
    *   **实时状态**：显示当前活动图标、预计到达时间、距离目标距离及剩余时间。
    *   **波形动画**：在收起状态下，显示音频波形动画，模拟正在进行的活动状态。
    *   **交互**：展开后显示详细的进度条（Progress Bar），并提供“开始导航”按钮。
*   **数据流**：从 `trip.itinerary` 中扁平化提取所有时间线节点 (`allNodes`)，并按索引循环更新 `activeNode`。

---

## 2. 智能体与名片生成场景 (Agent & Card Generation)

该场景包含“个人名片”的数字化创建与“个人智能体”的 AI 辅助生成。

### 2.1 数字化名片创建 (Digital Business Card)
*   **入口**：`BusinessCardPage.jsx` -> `BusinessCardForm.jsx`
*   **流程**：
    1.  **信息录入**：用户填写姓名、职位、公司、联系方式及微信二维码。系统支持**默认数据填充**（王大雷/贵旅数网），方便演示。
    2.  **风格匹配**：用户上传或选择背景模版，前端模拟 AI 匹配过程（Loading 动画），自动调整名片视觉风格。
    3.  **预览与保存**：生成名片预览图，支持保存到本地存储 (`localStorage`)。
    4.  **分享**：提供微信分享预览 (`WeChatSharePreview`)，生成模拟的小程序卡片样式用于社交传播。

### 2.2 个人智能体创建 (Personal Agent Creation)
*   **入口**：`CreateAgentModal.jsx`
*   **流程**：
    1.  **AI 意图识别 (Step 1)**：
        *   用户输入自然语言描述（如：“我是专业的贵州本地向导...”）。
        *   点击 **“识别”** 按钮，系统根据关键词（导游/保洁等）自动锁定**服务类型**，并自动生成相关的**服务列表**（Mock 数据）。
    2.  **服务定价 (Step 2)**：
        *   用户可对 AI 生成的服务项目进行增删改。
        *   提供 **“参考价”** 功能，一键填入行业标准价格。
    3.  **人设美化 (Step 3)**：
        *   点击 **“AI 美化”**，系统根据选定的服务类型，自动生成一段专业、极具吸引力的**个人简介**文案。
    4.  **发布**：完成创建后，智能体将显示在首页或社交广场中（逻辑上存入 `localStorage`，由 `Home.jsx` 读取显示）。

---

## 3. 行程全链路场景 (Trip Lifecycle)

从用户模糊的旅行意图出发，到生成具体方案，再到执行与完结的全过程。

### 3.1 行程创建与生成 (Creation & Generation)
*   **交互载体**：`ChatInterface.jsx` (AI 聊天助手)
*   **流程**：
    1.  **意图表达**：用户发送消息（如：“我想去黄果树瀑布玩两天”）。
    2.  **AI 规划**：后端（Gemini 模型）生成结构化的行程 JSON 数据，包含每日的时间线、地点、活动类型。
    3.  **卡片推送**：聊天界面渲染 `TripCard`，展示行程概览。
    4.  **采纳行程**：用户点击“采纳”，行程数据被提升为全局状态 (`adoptedTrip`)。

### 3.2 行程执行与伴游 (Execution)
*   **交互载体**：`TripDetail.jsx` & `DynamicIsland.jsx`
*   **流程**：
    1.  **详情展示**：行程详情页展示完整的时间轴，支持查看景点详情、酒店预订信息及 AI 生成的游玩贴士 (`Tips`)。
    2.  **灵动岛联动**：如场景 1.2 所述，灵动岛实时显示当前节点状态。
    3.  **倒计时**：首页显示距离行程开始的翻页倒计时 (`FlipCountdown`)。

---

## 4. 商品上架与展示场景 (Product Discovery)

目前的实现主要集中在商品的展示与发现阶段 (`Shop.jsx`)，侧重于“内容电商”的视觉呈现。

*   **主线流程**：
    1.  **沉浸式浏览**：用户进入商城，顶部为沉浸式 Header，首屏展示大图营销海报（如“村超联名款”）。
    2.  **分类筛选**：提供“甄选好物”、“非遗文创”等标签进行商品过滤。
    3.  **商品卡片**：展示商品图片、价格、评分及标签（如“非遗”、“甄选”）。
    4.  **营销专区**：
        *   **榜单推荐**：展示“黄小西甄选榜单”。
        *   **限时特惠**：展示倒计时促销活动。

> **注**：当前的“分销完成”环节（如下单、支付、佣金结算）在代码中尚未包含完整逻辑，目前主要体现为前端的视觉展示与交互框架。

---

## 5. 多智能体路由与调度场景 (Multi-Agent Routing & Dispatch)

该场景体现了“黄小西”作为**总调度智能体 (Dispatcher)** 的核心能力，即根据用户意图将对话无缝路由给垂直领域的专业智能体。

### 5.1 总调度模式 (Dispatcher Mode)
*   **核心逻辑**：所有对话默认由“黄小西”接待。她负责意图识别，但不直接处理具体的业务预订（如订房、订车），而是作为“连接器”。
*   **路由触发**：
    *   **自然语言触发**：用户说“我想订个车” -> 黄小西推荐“交通出行智能体”。
    *   **上下文触发**：用户在“智能体广场”点击某个特定智能体（如“金牌导游小张”）进入对话 -> 系统携带 `initialContext` 自动触发路由推荐。
*   **表现形式**：
    *   黄小西会发送一张 **“智能体推荐卡片” (AgentRecommendationCard)**。
    *   卡片展示专业智能体的头像、评分、服务标签。
    *   用户点击“立即联系”或“对话”按钮，完成路由跳转。

### 5.2 垂直智能体服务模式 (Service Agent Mode)
*   **状态切换**：用户确认连接后，`ChatInterface` 的 `activeAgent` 状态更新。
*   **界面变化**：
    *   **顶部栏 (Header)**：颜色、图标、标题从“黄小西”切换为对应智能体的主题色（如：酒店-靛蓝、餐饮-橙色、交通-绿色）。
    *   **服务卡片**：聊天区域不再是通用对话，而是弹出该智能体专属的**结构化服务卡片**。
        *   **酒店智能体**：弹出 `HotelServiceCard`（选房型、填入住信息）。
        *   **餐饮智能体**：弹出 `DiningServiceCard`（选桌型、排队取号）。
        *   **景区智能体**：弹出 `ScenicServiceCard`（购票、预约讲解）。
        *   **交通智能体**：弹出 `TransportServiceCard`（选车型、用车时间）。
*   **自动填充 (Auto-fill)**：
    *   垂直智能体在拉起服务表单时，会自动从全局用户画像中提取姓名、电话、身份证号进行**预填充**，减少用户输入成本（界面显示“黄小西已为您自动填充”提示）。
