# 黄小西 V4.0 产品需求文档（PRD）

## 1. 文档信息
- **产品名称**：黄小西（文旅智能服务平台）
- **版本**：V4.0-R1
- **文档类型**：PRD（产品需求文档）
- **状态**：可评审版（已按三份 CSV 对齐）
- **目标平台**：小程序（老版架构迁移）+ 小程序开发 + App 容器（H5 嵌入）

## 2. 项目背景
黄小西 V4.0 的核心定位是打造覆盖“游前-游中-游后”全链条的贵州省旅游智能体服务生态：以黄小西为统一门户，通过问答模式连接旅行规划、旅居设计、旅行记录、酒店智能体、景区智能体、个人智能体、餐厅智能体等多类能力，为用户提供从决策到执行再到沉淀复用的一站式服务。

围绕该定位，本期重点解决以下业务问题：
1. **游前决策割裂**：攻略、问答、商品与规划结果分散，用户难以快速形成可执行行程。
2. **游中执行断层**：景区/酒店/餐饮/交通节点缺少统一服务承接，导航、购票、预订、问询路径长且跳转多。
3. **游后沉淀薄弱**：旅行记录、个人智能体和服务复用机制不足，优质经验难以沉淀和再次触发。
4. **多智能体协同不足**：不同智能体间账户、推荐、卡片字段、服务入口口径不统一，影响推荐准确性与转化效率。

因此，V4.0 将通过“统一账户（手机号）+ 统一问答入口 + 统一推荐规则 + 统一卡片字段与服务矩阵 + 行程状态机”打通首页、行程、问答、我的全链路，形成“可规划、可执行、可记录、可复用”的闭环旅游服务体系。

功能模块包括：首页门户、行程模块（我的行程/行程详情/地图/节点服务）、添加行程（创建新行程/扫码导入）、问答门户智能体、旅游规划智能体、旅居设计智能体、旅行记录智能体、的模块我；对接酒店智能体、景区智能体、餐厅智能体、个人智能体（数字分身）等团队，支持跳转到各智能体页面进行问答。

## 3. 目标与范围

### 3.1 核心目标
- 3.14之前完成小程序初版开发，3.20之前完成终版开发和测试。
- 3.30之前完成app开发和测试

### 3.2 本期范围（In Scope）
- 小程序老架构迁移（包含登录、支付能力接入位）。
- 账户打通：按手机号统一用户在各智能体中的账户。
- 首页、行程、问答、我的四大模块改造。
- 行程状态机（计划中/进行中/已完成）及冲突处理。
- 智能体推荐逻辑、卡片字段与服务矩阵落地。


## 4. 全局规则

### 4.1 账户与端形态
- 账号体系：手机号统一主键，打通智能体侧账号身份。
- 端形态：小程序进行原生开发；首页/行程/我的以 H5 形态嵌入 App，小程序与 H5 保持同口径字段。

## 5. 功能需求

### 5.1 首页（`/`）
**功能点**
- 顶部提示语支持后台配置。
- 黄小西图标状态与行程提醒卡片联动：
  - 卡片收起：趴着图标；
  - 卡片展开：站着图标；
  - 两个图标均支持后台配置。
- 行程提醒卡片规则：
  - 关联“已开始”的行程；
  - 展示进行中的行程节点（交通/美食/景区/酒店）；
  - 无进行中行程时显示“暂无进行中的行程”；
  - 节点状态显示“即将开始/进行中”。
- 公告入口：进入“原黄小西公告”二级页。
- 翻译入口：点击后进入翻译指引页。
- 智能体卡片展示规则：
  - 支持类型筛选和左右滑动堆叠展示，组内循环；
  - 精选固定展示：旅行规划、旅居设计、旅游记录 + 景区/酒店/餐厅排名前三智能体；
  - 地理位置开启时：酒店智能体取 20 公里内，按距离近到远；
  - 地理位置关闭时：按评分高到低。
  - 其他类型智能体按照评分展示
- 问答输入框：支持输入问题并带问题进入问答门户。
- 底部导航：首页/行程/添加行程（加号）/优选（金汇的h5页面）/我的。

**验收标准**
- 首页首次进入可完成定位权限分支处理：授权时按距离规则展示酒店智能体，未授权时按评分规则展示，且无报错。
- 行程提醒卡片与“我的行程开始/结束”状态同步延迟 <= 1s；无进行中行程时稳定显示缺省文案。
- 黄小西图标与行程提醒卡片状态联动正确（收起=趴着，展开=站着），图标资源支持后台替换后实时生效。
- 智能体卡片支持类型筛选与左右滑动堆叠，切换过程不刷新整页、不闪屏。
- 问答输入框可正确透传用户问题至问答门户，问题文本不丢失、不截断。
- 公告、翻译、底部导航、加号添加行程、优选 H5 入口均可正确跳转，返回路径符合小程序常规交互。

### 5.2 行程模块（`/trip`）

#### 5.2.1 精选线路
- 展示线路商品列表，点击进入线路详情。
- 数据来源：金汇商品数据。

#### 5.2.2 我的行程列表
- 数据来源：用户采纳行程和扫码导入的行程（一期的收藏功能）。
- 行程状态定义：`计划中` -> ` `进行中` ->已完成`（状态标签在卡片左上角展示）。
- 筛选条件：时间（当前年及前后各一年）+ 状态。
- 空状态文案：`暂无行程，快去规划你的第一次旅行吧`。
- 排序规则：
  1) 先按状态（进行中 > 计划中 > 已完成）；
  2) 再按时间由近到远。
- 卡片字段：状态标签、开始/提前结束按钮、行程标题（用户问题）、开始时间、行程天数、背景图（首个景点图，无则默认图）。

#### 5.2.3 开始/提前结束/重新开始行程规则
- 点击“开始行程”“重新开始”：
  - 需选择开始时间，选择成功后同步写入卡片；
  - 行程状态改为进行中并置顶；
  - 提示：`已开始xxx行程`。
- 点击“提前结束”：
  - 二次确认：`确定要提前结束该行程吗？结束行程后将停止行程提醒`；
  - 状态改为已完成并置底，按钮状态改为【重新开始】；
  - 多个已结束行程按时间近到远排序。
- 异常处理：
  - 选择时间晚于当前时间：提示 `该时间不可选`；
  - 存在其他进行中行程：提示 `已有在进行中的行程，请结束进行中的行程后再进行开始当前行程`。
- 联动逻辑：
  - 开始行程后：首页展示该行程首个节点；
  - 提前结束后：首页隐藏该行程提醒；
  - 详情页节点状态按时间流转（见 5.2.5）。

#### 5.2.4 删除行程规则
- 支持删除非进行中行程。
- 进行中行程不支持删除。
- 来源于“采纳行程”的删除后，问答按钮状态由 `已采纳` 变回 `一键采纳`。
- 来源于扫码导入的删除后，行程数据清空。

#### 5.2.5 行程详情
- 顶部总览地图：根据规划结果和用户修改后内容实时展示。
- 类型 Tab：全部/交通/餐饮/景点/住宿/自定义活动。
- 行程进行中时，详情页仅支持查看节点状态与服务触发，不支持编辑行程内容。
- 节点卡片：
  - 有智能体：优先展示智能体卡片（带 `智能体` 标签），点击卡片进入智能体问答页；
  - 无智能体：展示常规卡片（无智能体标签），点击进入详情页；各卡片展示内容见（5.2.6）。
- 节点状态流转规则：
  1) 点击开始行程后，第一个节点初始为未开始；
  2) 当前时间命中 A 节点时间，A 变进行中；
  3) 当前时间命中 B 节点时间，A 变已完成，B 变进行中；
  4) 当天最后节点在当天零点后变已结束，次日按同规则继续。
- 行程默认开始时间：沿用问答默认时间（默认次日开启）。

#### 5.2.6 节点卡片展示规则（字段表口径）
| 规则项 | 规则内容 |
|---|---|
| 适用页面 | 行程详情页 |
| 适用节点类型 | 交通（航班、火车、自驾）、景区/打卡、美食、餐饮（餐厅）、酒店、自定义活动、数字分身 |
| 通用展示字段 | 节点主标题、类型标签、核心字段摘要、AI Tips、智能体标识 |
| 字段显示条件 | AI Tips 有内容时展示；智能体标识在节点绑定智能体时展示 |
| 点击规则-有智能体 | 点击卡片进入对应智能体问答页 |
| 点击规则-无智能体且支持详情 | 点击卡片进入详情页 |
| 点击规则-无智能体且不支持跳转 | 卡片不可跳转；自定义活动按此规则执行 |
| 功能按钮结构 | 每个节点卡片统一采用两层功能区：通用功能、定制功能 |
| 通用功能集合 | 一键导航、电话咨询、购买门票、酒店订房、联系前台、查看地图、餐厅推荐 |
| 定制功能集合 | 游玩攻略、预定包厢、查看菜单、酒店服务、客房服务、周边推荐 |
| 定制功能触发方式 | 通过预置问题拉起智能体问答 |
| 无定制功能兜底 | 保留定制功能入口样式；点击后统一提示 `敬请期待` |

| 节点类型 | 核心字段 | 点击行为 | 通用功能 | 定制功能 |
|---|---|---|---|---|
| 交通（航班、火车、自驾） | 班次号、起终点、预计耗时、时间区间 | 进入详情页 | 一键导航 | 值机选座、改签退票、接送服务（本期不展示） |
| 景区/打卡 | 景区名称、景区等级、景区评分、门票价格、地理位置 | 有智能体进入智能体问答页；无智能体进入详情页 | 购买门票、一键导航、电话咨询 | 游玩攻略、智能导览 |
| 美食（无智能体） | 大众评分、人均消费、菜系标签 | 进入详情页 | 餐厅推荐 | 无（点击定制功能入口提示 `敬请期待`） |
| 餐饮（餐厅，有智能体） | 大众评分、星级、人均消费、地理位置 | 进入智能体问答页 | 一键导航、电话咨询 | 预定包厢、查看菜单 |
| 酒店 | 酒店名称、星级/钻级、地理位置 | 有智能体进入智能体问答页；无智能体进入详情页 | 酒店订房、一键导航、联系前台 | 酒店服务、客房服务、周边推荐 |
| 自定义活动 | 用户输入位置、用户输入内容 | 不可跳转 | 查看地图 | 无（点击定制功能入口提示 `敬请期待`） |
| 数字分身 | 展示位置：首页、问答页、行程详情节点扩展位 | 进入个人智能体会话 | 进入会话 | 按三级标签命中后触发个性化问答 |

#### 5.2.7 每日 Day 管理
- 支持 Day 维度 Tab 展示。
- 支持加天：在最后一天后顺延编号（如 Day3 -> Day4）。
- 支持删天：进入删除模式后点击 `X` 删除该天及节点，后续天自动重排编号。
- 至少保留一天行程。
- 行程进行中时，不支持增删 Day。

#### 5.2.8 添加行程节点
- 支持选择类型、时间、所属 Day。
- 与现有节点冲突时提示：`行程冲突，请修改时间。`
- 行程进行中时，不支持新增节点。
- 可添加的类型：景点、餐饮、酒店、自定义；交通本期不做

#### 5.2.9 R1 特殊约束
- R1 详情页仅展示有数据的景点/酒店/美食，展示顺序固定：景点 -> 酒店 -> 美食。
- R1 行程在我的行程列表中不支持“开始行程”。

### 5.3 添加行程入口
- 创建新行程：跳转旅行规划页面。
- 扫码导入：支持扫描旅行社二维码导入行程。

### 5.4 问答体系（门户与垂类智能体）

#### 5.4.1 问答门户智能体
- 规划问答答案结构：文本 + 地图 + 卡片混排（智能体卡片/商品卡片）+ 功能键。
- 卡片规则：有智能体时优先展示智能体卡片（带 `智能体` 标签）并进入智能体问答页；无智能体进入详情页（或按模块约束不跳转）。
- 功能键：点赞/点踩/复制/查看行程/一键购买/采纳行程。
- `采纳行程` 后按钮改为 `已采纳`，并写入“我的行程”。
- 订购问答能力：
  - 景区：提问后展示卡片并跳转景区详情页；
  - 酒店：提问后展示卡片并跳转酒店详情页；
  - 火车票：直接跳转火车票订购页（机票暂不支持）；
  - 餐厅：展示餐厅智能体/商品卡片，不自动跳转。
- 推荐类问答：文本 + 卡片混排 + 轻操作功能键（点赞/点踩/复制）。

#### 5.4.2 旅游规划智能体
- 答案结构与门户一致。
- 输入框上方快捷按钮：
  - 启发我去哪、风味贵州：触发对话；
  - 著名景点：触发“景点智能体 + 商品卡片”混排结果；
  - 景区导览：跳转原有 H5 页面。

#### 5.4.3 旅居设计智能体
- 答案结构与门户一致。
- 快捷按钮（旅居线路/旅居基地/旅居攻略/旅居舒适度）跳转对应 H5 页面。

#### 5.4.4 旅行记录智能体
- 答案结构与门户一致。
- 快捷按钮“打卡留言”跳转既有 H5 页面。

### 5.5 我的（`/profile`）
- 个人信息、我的服务、常用证件、协议规则、设置与原功能保持一致。
- 热线电话：弹窗展示服务热线与黄小西客服电话。
- 我的智能体：
  - 无个人智能体：展示“创建个人智能体”入口并跳转创建 H5；
  - 单用户仅允许创建 1 个个人智能体；
  - 已创建后展示卡片并支持：推送、查看详情、删除。

## 6. 智能体推荐逻辑（核心规则）

### 6.1 规划类推荐
- 景点/特色打卡地：根据用户问题和规划器结果按行程顺序展示；有智能体优先展示智能体卡片。
- 酒店/民宿：按“景点到酒店距离 <= 10km”筛选，近到远推荐 3 家。
- 餐厅：按“中午最后一个景点或当天最后一个景点所在区县”推荐当地特色美食。
- 文娱/民俗活动：当问题命中“民俗活动/文娱活动”等关键词时，按活动位置和时间纳入行程。

### 6.2 推荐类问答推荐
- 景点：POI 圆心 60km 内，按评分高到低推荐 Top10；行政区场景按景点综合评分 Top10。
- 酒店：POI 圆心 10km 内，按距离近到远 Top10；行政区场景按评分高到低 Top10。
- 餐厅/美食：POI 圆心 50km 内，按距离近到远 Top10；行政区场景按评分高到低 Top10。
- 文娱/民俗：POI 圆心 60km 内按评分 Top5；行政区按热度高到低 Top5。
- 个人智能体（数字分身）：以三级标签命中为准，用户问题经大模型识别后进行一二三级标签精准匹配，命中即推荐。

## 7. 卡片字段与服务矩阵（字段表落地）

### 7.1 交通（航班/火车/自驾）
- 本期是否有智能体：否。
- 核心字段：班次号、起终点、预计耗时/时间区间。
- Tips 示例：`站内换乘预留20分钟`、`历史准点率95%`。
- 点击行为：进入详情页。
- 基础服务：一键导航（到机场/车站）。
- 定制服务：值机选座/改签退票/接送服务（本期不展示）。

### 7.2 景区/打卡
- 本期是否有智能体：是。
- 核心字段：景区名称、等级、评分、门票价格、地理位置。
- 标签示例：打卡地、亲子、徒步。
- 基础服务：购买门票、一键导航、电话咨询。
- 定制服务：智能导览（AR/语音伴游）。

### 7.3 美食（无智能体）
- 核心字段：评分、人均消费、菜系标签。
- 基础服务：餐厅推荐（进入详情页）。
- 点击行为：进入详情页。

### 7.4 餐饮（有智能体）
- 核心字段：评分、星级、人均、位置。
- 基础服务：一键导航、电话咨询。
- 定制服务：预定包厢、查看菜单（以预置问题拉起智能体问答）。
- 说明：餐厅本期无独立详情页。

### 7.5 酒店
- 核心字段：酒店名、星级/钻级、地理位置。
- 基础服务：一键导航、联系前台。
- 定制服务：设施问询、客房服务、周边推荐（预置问题拉起智能体问答）。

### 7.6 自定义活动
- 本期是否有智能体：否。
- 核心字段：用户输入位置与活动内容。
- 点击行为：不可跳转。
- 基础服务：查看地图（定位到用户当前位置）。

### 7.7 数字分身
- 本期是否有智能体：是。
- 展示位置：首页、问答页。
- 推荐命中方式：三级标签精准匹配。

## 8. 非功能需求
- **性能**：首页首屏可交互时间 <= 2.5s（4G 环境）。
- **稳定性**：开始/结束行程、采纳行程、推荐卡片召回等关键链路成功率 >= 95%。
- **一致性**：同一节点在首页、问答、行程详情的字段口径一致。
- **安全与隐私**：手机号作为统一账号键，需脱敏存储与传输。
- **兼容性**：小程序、H5、App 容器三端一致可用。

## 9. 数据埋点与监控
- 行程状态埋点：开始/结束/冲突拦截/删除。
- 节点状态埋点：未开始 -> 进行中 -> 已完成/已结束的状态流转。
- 推荐效果埋点：召回量、曝光量、点击率、转化率（按类型/规则拆分）。
- 卡片服务埋点：基础服务与定制服务点击率、成功率。
- 问答埋点：采纳行程率、一键购买触发率、订购类问答跳转成功率。

## 10. 风险与依赖
- **地理位置依赖**：位置关闭时需平滑降级到评分排序。
- **多方系统依赖**：金汇、贵旅数网、地图、电话能力需按接口进度联调。
- **规则复杂度风险**：行程状态机和节点时间规则易产生边界问题，需重点回归。
- **数据质量风险**：POI、评分、热度、标签缺失会影响推荐准确性。

## 11. 里程碑建议
- **M1（规则冻结）**：字段表、推荐逻辑、状态机口径冻结。
- **M2（核心联调）**：首页卡片联动 + 行程状态机 + 问答采纳链路打通。
- **M3（推荐上线）**：POI/行政区推荐规则全量上线并灰度验证。
- **M4（验收复盘）**：指标复盘并输出 V4.1 优化项。

## 12. 附录：关键提示文案
- 开始成功：`已开始xxx行程`
- 时间非法：`该时间不可选`
- 行程冲突：`已有在进行中的行程，请结束进行中的行程后再进行开始当前行程`
- 结束确认：`确定要提前结束该行程吗？结束行程后将停止行程提醒`
- 节点冲突：`行程冲突，请修改时间。`
- 无定制功能：`敬请期待`
